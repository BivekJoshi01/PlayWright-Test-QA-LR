# Copy this file to `.env` and fill in real values. `.env` is gitignored.

# Where the tests run. Default is hosted dev.
PLAYWRIGHT_BASE_URL=https://dev.legalremit.com

# An existing user on the target environment with 2FA disabled.
TEST_USER_EMAIL=
TEST_USER_PASSWORD=
